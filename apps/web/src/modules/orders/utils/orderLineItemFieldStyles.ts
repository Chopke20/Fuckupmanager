/** Placeholder w polu nazwy pozycji (sprzęt, produkcja). */
export const orderLineNamePlaceholder = 'Nazwa - wpisz ręcznie lub wybierz z listy'

/** Nazwa pozycji — widoczne pole (box). */
export const orderLineNameInputClass =
  'w-full min-w-0 px-2 py-1 text-xs bg-background border border-border rounded'

/** Opis pozycji — bez boxa, tekst pod nazwą. */
export const orderLineDescriptionInputClass =
  'w-full min-w-0 px-2 py-0.5 text-xs bg-transparent border border-transparent hover:border-border rounded focus:border-primary focus:outline-none text-muted-foreground mt-1'
